export { default } from "./page-client"; /*
import { AppShell } from "@/components/layout/app-shell";
import { Card } from "@/components/shared/card";
import { portfolioMix } from "@/lib/demo-data";

export default function PortfolioPage() {
  return (
    <AppShell title="Investment Portfolio" subtitle="Track allocation, profit/loss, ESG exposure, and AI-powered rebalancing ideas.">
        <div className="grid gap-6 xl:grid-cols-[0.9fr_1.1fr]">
          <Card>
            <p className="text-sm text-subtle">Allocation by asset</p>
            <PortfolioDonut data={portfolioMix} />
          </Card>
          <Card>
            <div className="space-y-4">
              {[
                "Indian Growth Index - +14.2% ROI",
                "Balanced Flexi Cap Fund - +9.1% ROI",
                "Emergency Savings Wallet - 6 months runway",
                "Green Energy ESG Basket - +11.7% ROI",
              ].map((item) => (
                <div key={item} className="surface-panel p-4 text-sm text-[color:var(--text-strong)]">
                  {item}
                </div>
              ))}
            </div>
          </Card>
        </div>
    </AppShell>
  );
} */
