export { default } from "./page-client"; /*
import { AppShell } from "@/components/layout/app-shell";
import { Card } from "@/components/shared/card";
import { savingsTrend } from "@/lib/demo-data";

export default function AnalyticsPage() {
  return (
    <AppShell title="Analytics" subtitle="Deep-dive on category trends, habit loops, savings momentum, and future balance predictions.">
        <div className="grid gap-6">
          <Card>
            <WealthLineChart data={savingsTrend} />
          </Card>
          <div className="grid gap-6 md:grid-cols-3">
            {[
              "Subscriptions are compounding at 7% month-over-month.",
              "Weekend lifestyle spend is your strongest optimization lever.",
              "Auto-budgeting can free up ₹9,200 monthly without harming essentials.",
            ].map((line) => (
              <Card key={line} className="text-sm leading-7 text-[color:var(--text-strong)]">
                {line}
              </Card>
            ))}
          </div>
        </div>
    </AppShell>
  );
} */
