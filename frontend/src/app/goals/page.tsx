export { default } from "./page-client"; /*
import { Card } from "@/components/shared/card";

export default function GoalsPage() {
  return (
    <AppShell title="Goal Planner" subtitle="Create outcomes, simulate timelines, and let AI convert them into executable monthly wealth plans.">
        <div className="grid gap-6 lg:grid-cols-3">
          {[
            { title: "Europe Trip", progress: "62%", note: "Save ₹18,500 monthly to arrive 2 months early." },
            { title: "Emergency Fund", progress: "74%", note: "Current cash reserve covers 4.5 months." },
            { title: "Home Down Payment", progress: "21%", note: "Feasibility score 78 with moderate SIP increase." },
          ].map((goal) => (
            <Card key={goal.title}>
              <p className="text-sm text-subtle">{goal.title}</p>
              <p className="mt-4 font-display text-4xl text-[color:var(--text-strong)]">{goal.progress}</p>
              <div className="mt-4 h-3 rounded-full bg-[color:var(--surface-strong)]">
                <div className="h-3 rounded-full bg-gradient-to-r from-mint to-ocean" style={{ width: goal.progress }} />
              </div>
              <p className="mt-4 text-sm leading-7 text-muted">{goal.note}</p>
            </Card>
          ))}
        </div>
    </AppShell>
  );
} */
