export { default } from "./page-client"; /*
import { Card } from "@/components/shared/card";

export default function NotificationsPage() {
  return (
    <AppShell title="Notification Center" subtitle="Stream live alerts for fraud, overspending, goal milestones, and AI intervention opportunities.">
        <div className="space-y-4">
          {[
            "Fraud alert: unusually large debit attempted at 02:13 AM.",
            "Goal update: travel fund crossed 60% completion.",
            "Behavior nudge: discretionary spend trending above your weekly budget.",
          ].map((item) => (
            <Card key={item} className="text-sm leading-7 text-[color:var(--text-strong)]">
              {item}
            </Card>
          ))}
        </div>
    </AppShell>
  );
} */
