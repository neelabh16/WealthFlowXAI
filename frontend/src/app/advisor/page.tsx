export { default } from "./page-client"; /*
import { AppShell } from "@/components/layout/app-shell";
import { advisorMessages } from "@/lib/demo-data";

export default function AdvisorPage() {
  return (
    <AppShell title="AI Advisor" subtitle="Ask contextual questions about affordability, waste, budgets, goals, and investment timing.">
      <ChatWindow messages={advisorMessages} />
    </AppShell>
  );
} */
