export { default } from "./page-client"; /*
import { Card } from "@/components/shared/card";
import { transactionRows } from "@/lib/demo-data";

export default function TransactionsPage() {
  return (
    <AppShell title="Transactions" subtitle="Search, edit, categorize, and monitor every money movement with AI-assisted context.">
        <Card>
          <div className="mb-6 flex flex-wrap gap-3 text-sm text-muted">
            {["All", "Income", "Expense", "Flagged", "Subscriptions", "Last 30 days"].map((pill) => (
              <span key={pill} className="rounded-full border border-[color:var(--border)] px-4 py-2">
                {pill}
              </span>
            ))}
          </div>
          <div className="space-y-4">
            {transactionRows.concat(transactionRows).map((row, index) => (
              <div key={`${row.merchant}-${index}`} className="surface-panel grid gap-4 p-4 md:grid-cols-4">
                <div>
                  <p className="font-semibold text-[color:var(--text-strong)]">{row.merchant}</p>
                  <p className="text-sm text-subtle">{row.category}</p>
                </div>
                <p className="text-sm text-muted">Auto-categorized by ML with subscription leak scoring</p>
                <p className="font-semibold text-[color:var(--text-strong)]">{row.amount}</p>
                <p className="text-sm text-[color:var(--brand)]">{row.status}</p>
              </div>
            ))}
          </div>
        </Card>
    </AppShell>
  );
} */
